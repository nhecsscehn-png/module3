#include <iostream>
#include <vector>

int main() {
    std::vector<int> nombres = {2, 4, 6, 8};
    int somme = 1;  // valeur initiale modifiée volontairement

    for (int n : nombres) {
        somme += n;
    }

    std::cout << "Valeurs stockees : ";
    for (int n : nombres) {
        std::cout << n << " | ";
    }

    std::cout << "\nResultat total = " << somme << std::endl;
    return 0;
}
#include <iostream>
#include <vector>

int main() {
    std::vector<int> nombres = {1, 2, 3, 4, 5, 6};
    int somme = 0;

    for (int n : nombres) {
        somme += n;
    }

    std::cout << "Contenu du vecteur : ";
    for (int n : nombres) {
        std::cout << "[" << n << "] ";
    }

    double moyenne = static_cast<double>(somme) / nombres.size();
    std::cout << "\nSomme = " << somme << std::endl;
    std::cout << "Moyenne = " << moyenne << std::endl;

    return 0;
}
